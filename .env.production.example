## Production bundle config — copy to `.env.production` (gitignored).
##
## Used by `npm run build` and `npm run deploy`. Overrides `.env` for production builds.
## See also `.env.example` (development default) and `.env.development` (local Apache).

# API base URL (no trailing slash on path roots; include scheme)
API_URL=https://api.camelbird.com
